import { HighlightRadioDirective } from './highlight-radio.directive';

describe('HighlightRadioDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightRadioDirective();
    expect(directive).toBeTruthy();
  });
});
